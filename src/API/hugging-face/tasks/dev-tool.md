# hugging-face api cli tool

## start implementation

writes a python cli tool that allows to call the hugging face hub, using libray `huggingface_hub` and `ModelCard`, in order to get a `model card`. 

the name of the model is given as a `cli tool argument`

the cli tool executable has the name: `get-model-card`
