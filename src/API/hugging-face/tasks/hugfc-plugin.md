# plugin: `hugging-face`

Implements a new plugin named `hugging-face` (name of the folder in `/plugins`), according to the specification in file `plugins/tasks/dev-plugin.md`. 

The `pluginId` is: `huggingFace`

Write the result in the appropriate files in the folder `plugins/hugging-face/plugin/`. 

Update the property `cli.plugins` in file `plugins/hugging-face/plugin/config/config.js` to include the plugin specification. Add the file and the property if they are missing. 

Implements a plugin implementation class in the folder `plugins/hugging-face/plugin/plugin/`

Use as a model the example class provided in the file `plugins/tasks/dev-plugin.md`
