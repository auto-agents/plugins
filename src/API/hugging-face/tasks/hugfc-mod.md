# module: `hugging-face`

Implements a new module named `hugging-face` (name of the folder in `/modules`), according to the specification in file `modules/tasks/dev-module.md`. 

The `moduleId` is: `huggingFace`

Write the result in the appropriate files in the folder `modules/hugging-face/module/`. 

Update the property `cli.modules` in file `modules/hugging-face/module/config/config.js` to include the module specification. Add the file and the property if they are missing. 

Implements a module implementation class in the folder `modules/hugging-face/module/module/`

Use as a model the example class provided in the file `modules/tasks/dev-module.md`
